# Google Calendar API Configuration
# Create a project in Google Cloud Console, enable the Calendar API, and create credentials
# More info: https://developers.google.com/calendar/api/quickstart/nodejs

# Your Google Calendar API key
GOOGLE_CALENDAR_API_KEY=your_api_key_here

# The calendar ID you want to access
# Use 'primary' for the primary calendar of the authenticated user
# Or use a specific calendar ID (usually in format: abc123@group.calendar.google.com)
GOOGLE_CALENDAR_ID=primary

# Optional: Timezone for date/time display (defaults to system timezone)
# TZ=Asia/Bangkok